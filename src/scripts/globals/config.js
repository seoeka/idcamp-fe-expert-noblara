const config = {
  BASE_URL: 'https://restaurant-api.dicoding.dev/',
  BASE_IMAGE_URL: 'https://restaurant-api.dicoding.dev/images/medium/',
  CACHE_NAME: `Noblara-${new Date().toISOString()}`,
};

export default config;
